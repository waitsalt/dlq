pub struct Post {
    pub id: u16,
    pub title: String,
    pub uploader: u16,
    pub contest: String,
    // pub tag:Vec<String>,
    pub class: String,
    pub created: String,
    pub update: String,
    pub user: String,
}
